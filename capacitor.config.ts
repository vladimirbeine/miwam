import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wappinesslab.miwam',
  appName: 'miwam',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
